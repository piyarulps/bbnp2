import { ExtractDayPipe } from './extract-day.pipe';

describe('ExtractDayPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractDayPipe();
    expect(pipe).toBeTruthy();
  });
});
